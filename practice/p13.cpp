/*Word Count: Create a program that receives a sentence from the user and counts the number of words in the sentence.
 A word is defined as a sequence of characters separated by spaces.*/
 #include<iostream>
 using namespace std;
 int main () {
    cout << "Input a string to count words. \n";
    string input;
    getline(cin, input);
    int count = 0;
    while (count < input.length()) {
        
    }
 }